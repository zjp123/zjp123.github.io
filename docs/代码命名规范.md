# 代码命名的艺术：前端变量与函数命名实战手册

> 作者：资深前端架构师 · 15 年实战经验总结

---

## 📖 核心哲学

### 为什么"命名"如此重要？

Phil Karlton 曾说过一句经典名言：

> "计算机科学中只有两件难事：缓存失效和命名。"

这并非玩笑。在大型前端项目中，**代码被阅读的次数远超编写次数**。一个糟糕的命名可能让后续维护者（包括 6 个月后的你自己）花费数倍时间理解代码意图。

### 好的命名即文档

当你的变量名足够精准时，你会发现：
- ✅ 代码注释变得多余
- ✅ 函数职责一目了然
- ✅ 代码审查速度提升 50%+
- ✅ Bug 定位时间显著缩短

**核心原则**：花 3 分钟思考命名，能为团队节省 3 小时的沟通成本。

---

## 🎯 变量命名的 4 大黄金法则

### 1. 精准性 (Precision) —— 拒绝模糊词汇

#### ❌ 坏示例：过于宽泛的命名

```typescript
// 这是什么数据？用户？订单？配置？
const data = await fetchApi('/users');

// info 同样含义模糊
const info = user.profile;

// item 在列表渲染中常见，但缺乏语义
items.map(item => <div>{item.name}</div>);
```

#### ✅ 好示例：具体化命名

```typescript
// 明确指出是用户列表数据
const userList = await fetchUserList();

// 清晰表达是用户资料信息
const userProfile = user.profile;

// 列表项也应体现业务含义
productList.map(product => <ProductCard key={product.id} {...product} />);
```

**实战建议**：
- 用 `user` 代替 `data`
- 用 `orderTotal` 代替 `total`（当页面有多个总计时）
- 用 `activeTabIndex` 代替 `index`

---

### 2. 意图导向 (Intent) —— 描述"是什么"而非"怎么做"

#### ❌ 坏示例：暴露实现细节

```typescript
// 变量名暴露了数据结构，而非业务含义
const ageArray = [25, 30, 35, 40];

// 关注了技术实现，而非业务意图
const filteredData = users.filter(u => u.status === 'active');
```

#### ✅ 好示例：表达业务意图

```typescript
// 描述业务概念，而非数据结构
const employeeAges = [25, 30, 35, 40];

// 表达业务状态，即使底层是过滤实现
const activeUsers = users.filter(u => u.status === 'active');
```

**React 状态管理中的实践**：

```typescript
// ❌ 暴露技术细节
const [flag, setFlag] = useState(false);
const [count, setCount] = useState(0);

// ✅ 表达业务意图
const [isModalOpen, setIsModalOpen] = useState(false);
const [unreadNotificationCount, setUnreadNotificationCount] = useState(0);
```

---

### 3. 可搜索性 (Searchability) —— 让代码易于查找

#### ❌ 坏示例：缩写与魔法数字

```typescript
// 缩写导致搜索困难
const u = getCurrentUser();
const msg = fetchMessages();

// 魔法数字缺乏语义
if (user.age > 18) {
  // ...
}

setTimeout(() => {
  pollData();
}, 5000);
```

#### ✅ 好示例：完整命名与常量化

```typescript
// 完整命名，便于全局搜索
const currentUser = getCurrentUser();
const messageList = fetchMessages();

// 常量化魔法数字
const LEGAL_AGE = 18;
if (user.age > LEGAL_AGE) {
  // ...
}

const POLL_INTERVAL_MS = 5000;
setTimeout(() => {
  pollData();
}, POLL_INTERVAL_MS);
```

**常量命名规范**：

```typescript
// 全局配置常量：大写 + 蛇形命名
const API_BASE_URL = 'https://api.example.com';
const MAX_UPLOAD_FILE_SIZE = 5 * 1024 * 1024; // 5MB

// 枚举值：PascalCase
enum UserRole {
  Admin = 'admin',
  Editor = 'editor',
  Viewer = 'viewer',
}
```

---

### 4. 上下文对称 (Symmetry) —— 配对使用提升认知

#### 常见对称词汇表

| 场景 | 对称词对 | 示例 |
|------|---------|------|
| 时间范围 | `start` / `end` | `startDate`, `endDate` |
| 数值边界 | `min` / `max` | `minPrice`, `maxPrice` |
| 状态控制 | `open` / `close` | `openModal`, `closeModal` |
| 数据操作 | `get` / `set` | `getUser`, `setUser` |
| 列表操作 | `add` / `remove` | `addItem`, `removeItem` |
| 显示状态 | `show` / `hide` | `showDialog`, `hideDialog` |
| 生命周期 | `create` / `destroy` | `createConnection`, `destroyConnection` |

#### ✅ 实战示例

```typescript
// 时间范围过滤
interface DateRangeFilter {
  startDate: Date;
  endDate: Date;
}

// 价格区间筛选
interface PriceRange {
  minPrice: number;
  maxPrice: number;
}

// 对称的事件处理
const handleModalOpen = () => setIsModalOpen(true);
const handleModalClose = () => setIsModalOpen(false);
```

---

## 🚀 前端特定场景实战

### 1. 布尔值命名：语义前缀的细微差别

#### `is` —— 表示状态

用于描述组件或数据的**当前状态**。

```typescript
const isLoading = false;
const isActive = true;
const isVisible = user.role === 'admin';
const isModalOpen = false;
```

#### `has` —— 表示拥有/包含

用于描述**拥有某个属性或包含某个元素**。

```typescript
const hasError = errors.length > 0;
const hasChildren = node.children.length > 0;
const hasPermission = user.permissions.includes('edit');
const hasUnreadMessages = unreadCount > 0;
```

#### `should` —— 表示条件/策略

用于**配置项或策略判断**。

```typescript
const shouldAutoSave = config.autoSave;
const shouldShowWarning = errors.length > 5;
const shouldFetchData = !cache.has(key);
```

#### `can` —— 表示能力/权限

用于描述**是否有能力执行某个操作**。

```typescript
const canEdit = user.role === 'admin' || user.id === post.authorId;
const canDelete = hasPermission('delete');
const canSubmit = form.isValid && !isSubmitting;
```

#### 完整示例对比

```typescript
interface UserState {
  // 状态
  isLoggedIn: boolean;
  isEmailVerified: boolean;
  
  // 拥有
  hasAvatar: boolean;
  hasPremiumSubscription: boolean;
  
  // 能力
  canPostComments: boolean;
  canAccessAdminPanel: boolean;
  
  // 策略
  shouldReceiveNewsletter: boolean;
  shouldEnableTwoFactor: boolean;
}
```

---

### 2. 函数/方法命名：动词 + 名词模式

#### 核心规则：`动作 + 宾语`

```typescript
// ✅ 清晰的动词前缀
function fetchUserList() { /* ... */ }
function createOrder() { /* ... */ }
function validateEmail(email: string) { /* ... */ }
function transformData(raw: RawData) { /* ... */ }
function calculateTotal(items: CartItem[]) { /* ... */ }
```

#### 常见动词分类

**数据获取类**：
```typescript
// fetch - 远程获取（异步）
async function fetchProducts(): Promise<Product[]> { }

// get - 本地获取（同步）
function getActiveUsers(users: User[]): User[] { }

// retrieve - 从缓存/存储中恢复
function retrieveFromLocalStorage(key: string): unknown { }
```

**数据修改类**：
```typescript
// update - 部分更新
function updateUserProfile(userId: string, changes: Partial<User>) { }

// set - 完全覆盖
function setTheme(theme: 'light' | 'dark') { }

// modify - 修改（强调改变）
function modifyPermissions(userId: string, permissions: string[]) { }
```

**数据转换类**：
```typescript
// transform - 结构性转换
function transformApiResponse(raw: ApiData): UiData { }

// convert - 类型转换
function convertToCSV(data: object[]): string { }

// format - 格式化
function formatCurrency(amount: number): string { }
```

**验证/检查类**：
```typescript
// validate - 完整验证（返回布尔或错误）
function validateForm(form: FormData): ValidationResult { }

// check - 快速检查（返回布尔）
function checkPermission(user: User, action: string): boolean { }

// verify - 验证真实性
function verifyToken(token: string): boolean { }
```

---

### 3. React/Vue 组件命名：Props 与事件处理

#### Props 命名规范

```typescript
// ✅ Props 应该是名词或形容词
interface ButtonProps {
  // 状态属性：形容词
  disabled?: boolean;
  loading?: boolean;
  
  // 数据属性：名词
  label: string;
  icon?: ReactNode;
  variant?: 'primary' | 'secondary';
  
  // 事件回调：on + 动作
  onClick?: () => void;
  onSubmit?: (data: FormData) => void;
}

// ❌ 不要在 Props 中使用动词开头（除了回调）
interface BadProps {
  clickButton?: () => void;  // 应该是 onClick
  submitForm?: () => void;   // 应该是 onSubmit
}
```

#### 事件处理：`on` vs `handle`

```typescript
// 规则：
// - Props 中的回调用 `on` 前缀
// - 组件内部的处理函数用 `handle` 前缀

interface ModalProps {
  // 传入的回调：on 前缀
  onClose?: () => void;
  onSubmit?: (data: FormData) => void;
}

function Modal({ onClose, onSubmit }: ModalProps) {
  // 内部处理函数：handle 前缀
  const handleFormSubmit = (e: FormEvent) => {
    e.preventDefault();
    const formData = new FormData(e.target);
    onSubmit?.(formData);
  };
  
  const handleBackdropClick = () => {
    if (confirm('确定关闭？')) {
      onClose?.();
    }
  };
  
  return (
    <div onClick={handleBackdropClick}>
      <form onSubmit={handleFormSubmit}>
        {/* ... */}
      </form>
    </div>
  );
}
```

#### Vue 中的事件命名

```typescript
// Vue 组件事件使用 kebab-case
<template>
  <CustomButton 
    @button-click="handleButtonClick"
    @form-submit="handleFormSubmit"
  />
</template>

<script setup lang="ts">
// 定义事件
const emit = defineEmits<{
  'button-click': [];
  'form-submit': [data: FormData];
}>();

// 内部处理
const handleButtonClick = () => {
  emit('button-click');
};
</script>
```

---

### 4. CSS-in-JS / 类名：BEM 思想在变量中的体现

#### Styled Components 变量命名

```typescript
import styled from 'styled-components';

// ✅ 组件名：PascalCase + 语义化后缀
const CardContainer = styled.div`
  padding: 16px;
`;

const CardTitle = styled.h3`
  font-size: 18px;
`;

const CardDescription = styled.p`
  color: #666;
`;

// 使用 BEM 思想组织修饰符
const Button = styled.button<{ variant?: 'primary' | 'secondary' }>`
  ${props => props.variant === 'primary' && `
    background: blue;
  `}
`;

// ✅ 组合使用
function ProductCard() {
  return (
    <CardContainer>
      <CardTitle>商品名称</CardTitle>
      <CardDescription>商品描述</CardDescription>
      <Button variant="primary">购买</Button>
    </CardContainer>
  );
}
```

#### Tailwind / 原生 CSS 类名变量

```typescript
// ✅ 类名对象：camelCase
const buttonStyles = {
  base: 'px-4 py-2 rounded',
  primary: 'bg-blue-500 text-white hover:bg-blue-600',
  secondary: 'bg-gray-200 text-gray-800 hover:bg-gray-300',
};

// ✅ 类名组合函数
function getButtonClassName(variant: 'primary' | 'secondary'): string {
  return `${buttonStyles.base} ${buttonStyles[variant]}`;
}
```

---

## ⚠️ 反面教材 (Anti-patterns)

### 1. 单字母变量滥用

#### ❌ 问题代码

```typescript
const u = users.find(u => u.id === id);
const e = employees.filter(e => e.status === 'active');

// 嵌套循环中的灾难
data.forEach(d => {
  d.items.forEach(i => {
    i.tags.forEach(t => {
      // t 是什么？已经完全迷失
    });
  });
});
```

#### ✅ 重构方案

```typescript
const targetUser = users.find(user => user.id === id);
const activeEmployees = employees.filter(employee => employee.status === 'active');

// 嵌套循环清晰化
orders.forEach(order => {
  order.items.forEach(item => {
    item.tags.forEach(tag => {
      console.log(`订单 ${order.id} 的商品 ${item.name} 有标签 ${tag}`);
    });
  });
});
```

**例外情况**：数学运算和通用迭代器

```typescript
// ✅ 可以接受的单字母
for (let i = 0; i < length; i++) { }  // 索引
const point = { x: 10, y: 20 };       // 坐标
const result = items.reduce((acc, item) => acc + item, 0);  // 累加器
```

---

### 2. 否定式布尔命名

#### ❌ 问题代码

```typescript
const isNotReady = false;
const isInvalid = true;

// 双重否定导致理解困难
if (!isNotReady) {
  // 什么？要读两遍才能理解
}
```

#### ✅ 重构方案

```typescript
const isReady = true;
const isValid = false;

// 清晰的逻辑
if (isReady) {
  startProcess();
}

if (!isValid) {
  showError();
}
```

---

### 3. 冗余的上下文重复

#### ❌ 问题代码

```typescript
// 对象内部重复类型名
const user = {
  userName: 'Alice',      // user 已经在变量名中了
  userEmail: 'alice@example.com',
  userAge: 25,
};

class UserService {
  getUserUser() { }       // 重复的 User
  createUserUser() { }
}
```

#### ✅ 重构方案

```typescript
// 去除冗余
const user = {
  name: 'Alice',
  email: 'alice@example.com',
  age: 25,
};

class UserService {
  getUser() { }
  createUser() { }
  updateProfile() { }     // 或者用更具体的业务术语
}
```

---

### 4. 过度缩写

#### ❌ 问题代码

```typescript
const usrMgr = new UserManager();
const cfg = loadConfig();
const btn = document.querySelector('.btn');
const msg = 'Hello';

// 团队自创的"黑话"
const po = productOwner;
const qa = qualityAssurance;
```

#### ✅ 重构方案

```typescript
const userManager = new UserManager();
const config = loadConfig();
const button = document.querySelector('.button');
const message = 'Hello';

// 明确的全称
const productOwner = getProductOwner();
const qualityAssuranceTeam = getQATeam();
```

**可接受的缩写**：

```typescript
// ✅ 行业通用缩写
const htmlContent = '<div>...</div>';
const apiUrl = 'https://api.example.com';
const idToken = 'jwt-token';
const maxValue = 100;
const minValue = 0;
```

---

### 5. 类型前缀匈牙利命名法（过时做法）

#### ❌ 问题代码

```typescript
// 90 年代的遗留风格
const strName = 'Alice';
const intAge = 25;
const boolIsActive = true;
const arrUsers = [];

// TypeScript 中完全多余
const stringUsername: string = 'test';
```

#### ✅ 重构方案

```typescript
// TypeScript 已经提供类型信息
const name = 'Alice';
const age = 25;
const isActive = true;
const users: User[] = [];

// 类型注解已经足够
const username: string = 'test';
```

---

## 🧰 命名工具箱

### 1. CODELF - 变量命名搜索引擎

**网址**：[https://unbug.github.io/codelf/](https://unbug.github.io/codelf/)

**功能**：
- 输入中文或英文关键词，搜索 GitHub 上真实项目的命名方式
- 支持多种编程语言
- 查看高星项目如何命名类似概念

**使用场景**：
```
搜索：用户列表
结果：userList, users, userCollection, userRegistry
选择：userList（最常见且清晰）
```

---

### 2. Better Comments (VSCode 插件)

**安装**：在 VSCode 扩展商店搜索 "Better Comments"

**功能**：
- 通过颜色高亮不同类型的注释
- 帮助识别需要重构的模糊命名

**配合使用**：
```typescript
// ! 这个变量名太模糊了，需要重构
const data = await fetchApi();

// TODO: 将 temp 重命名为 filteredProducts
const temp = products.filter(p => p.inStock);
```

---

### 3. ESLint 规则：强制命名规范

**安装配置**：

```bash
npm install -D eslint-plugin-unicorn
```

**`.eslintrc.js` 配置**：

```javascript
module.exports = {
  plugins: ['unicorn'],
  rules: {
    // 防止缩写
    'unicorn/prevent-abbreviations': ['error', {
      allowList: {
        props: true,  // React Props 允许
        ref: true,    // React Ref 允许
      }
    }],
    
    // 强制使用 isArray 而非 Array.isArray
    'unicorn/prefer-array-some': 'error',
    
    // 文件名必须用 kebab-case
    'unicorn/filename-case': ['error', {
      case: 'kebabCase',
    }],
  },
};
```

**团队约定示例**：

```javascript
// 自定义规则：布尔变量必须以 is/has/should/can 开头
rules: {
  'naming-convention': [
    'error',
    {
      selector: 'variable',
      types: ['boolean'],
      format: ['PascalCase'],
      prefix: ['is', 'has', 'should', 'can', 'will', 'did'],
    },
  ],
}
```

---

## 📚 延伸阅读

### 推荐书籍
- 《代码整洁之道》（Clean Code） - Robert C. Martin
- 《重构：改善既有代码的设计》 - Martin Fowler

### 在线资源
- [Google TypeScript Style Guide](https://google.github.io/styleguide/tsguide.html)
- [Airbnb JavaScript Style Guide](https://github.com/airbnb/javascript)
- [Vue.js Style Guide](https://vuejs.org/style-guide/)

---

## 🎯 最后的建议

### 命名的三重检查清单

在提交代码前，问自己三个问题：

1. **6 个月后的我能否一眼看懂？**
   - 如果需要猜测变量含义，就需要重构

2. **新同事能否在 30 秒内理解？**
   - 如果需要查看上下文才能理解，命名不够清晰

3. **能否在项目中快速搜索到？**
   - 如果搜索结果一大堆无关内容，命名过于通用

### 记住

> "代码是写给人看的，顺便让机器执行。"  
> —— 《计算机程序的构造和解释》

花在命名上的时间永远是值得的投资。一个好的命名能让代码自解释，减少文档负担，提升团队效率。

---

**Happy Coding! 愿你的变量名清晰如诗。** 🚀

