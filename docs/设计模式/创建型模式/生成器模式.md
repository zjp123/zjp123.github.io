# 《前端设计模式实战手册》

## 第三章：生成器模式（Builder Pattern）

---

### 🎯 一句话大白话

> **生成器模式就像麦当劳的"自选汉堡"：你可以一步步选择面包类型、肉饼数量、加不加芝士、要什么酱料……最后组装成一个完整的汉堡。你不需要记住复杂的配料表，只需要一步步选择，最后说"好了，给我做出来"。**

**核心理念：把复杂对象的"构建过程"和"最终产品"分离，通过一步步设置来创建复杂对象。**

---

### 🤔 和工厂模式有什么区别？

| 对比项 | 工厂模式 | 生成器模式 |
|--------|---------|-----------|
| **关注点** | 创建什么类型的对象 | 如何一步步构建对象 |
| **产品复杂度** | 通常是简单对象 | 通常是复杂对象，有多个可选配置 |
| **构建过程** | 一次性创建 | 分步骤构建 |
| **类比** | 去奶茶店点"珍珠奶茶" | 去奶茶店说"中杯、三分糖、加珍珠、加椰果、少冰" |
| **前端场景** | 根据 type 创建组件 | 构建复杂的表单配置、请求配置 |

**记住这个公式：**
```
工厂模式 = 告诉我要什么，我直接给你
生成器模式 = 你一步步告诉我细节，最后我帮你组装
```

---

### 😫 前端痛点：没有生成器时的噩梦

想象这个场景：你要创建一个 **复杂的 Modal 弹窗配置**：

```tsx
// ❌ 噩梦代码：超长的参数列表

// 方式一：一个函数传 N 个参数，根本记不住顺序
showModal(
  '确认删除',                    // title
  '确定要删除这条记录吗？',        // content
  true,                          // showCloseIcon
  false,                         // maskClosable  
  '删除',                        // okText
  '取消',                        // cancelText
  'danger',                      // okType
  true,                          // centered
  520,                           // width
  handleOk,                      // onOk
  handleCancel,                  // onCancel
  null,                          // footer - 等等，这是第几个参数来着？？
  // ... 还有更多
);

// 方式二：一个巨大的配置对象，写起来也很痛苦
Modal.confirm({
  title: '确认删除',
  content: '确定要删除这条记录吗？',
  icon: <ExclamationCircleOutlined />,
  okText: '删除',
  okType: 'danger',
  cancelText: '取消',
  centered: true,
  width: 520,
  maskClosable: false,
  keyboard: true,
  closable: true,
  closeIcon: <CloseOutlined />,
  onOk: handleOk,
  onCancel: handleCancel,
  // 每次都要写这么多，而且很容易漏掉或写错
});
```

**再看一个更复杂的场景：构建 API 请求配置**

```tsx
// ❌ 噩梦代码：每个请求都要写一堆重复配置

// 请求 1
const response1 = await axios({
  method: 'POST',
  url: '/api/users',
  headers: {
    'Content-Type': 'application/json',
    'Authorization': `Bearer ${token}`,
    'X-Request-Id': generateRequestId(),
    'X-Timestamp': Date.now(),
  },
  data: userData,
  timeout: 30000,
  withCredentials: true,
});

// 请求 2 - 又要写一遍几乎一样的配置
const response2 = await axios({
  method: 'GET',
  url: '/api/products',
  headers: {
    'Content-Type': 'application/json',
    'Authorization': `Bearer ${token}`,  // 重复
    'X-Request-Id': generateRequestId(), // 重复
    'X-Timestamp': Date.now(),           // 重复
  },
  params: { page: 1, size: 10 },
  timeout: 30000,  // 重复
  withCredentials: true,  // 重复
});
```

**这段代码有什么问题？**

| 问题 | 描述 |
|------|------|
| 🔴 **参数爆炸** | 参数太多，记不住顺序，容易传错位置 |
| 🔴 **可读性差** | 看到一堆 true/false/'danger'，不知道是什么意思 |
| 🔴 **重复代码多** | 很多配置是固定的，但每次都要写 |
| 🔴 **容易遗漏** | 忘记设置某个重要配置，出 bug 了才发现 |
| 🔴 **修改困难** | 要改默认配置？找遍所有使用的地方 |

---

### 💡 模式核心概念

生成器模式的核心思想是：

> **将一个复杂对象的构建与它的表示分离，使得同样的构建过程可以创建不同的表示。**

用大白话说就是：
- 不要一次性传入所有参数
- 提供一系列方法，每个方法设置一个或一组相关的属性
- 支持链式调用，写起来像"说话"一样自然
- 最后调用 `build()` 或 `create()` 生成最终对象

**类比理解：**

```
普通做法：去餐厅说"我要一个芝士牛肉汉堡，双层肉饼，不要洋葱，多加番茄酱，中份薯条，大杯可乐"
         → 一口气说完，容易说错或遗漏

生成器模式：
         店员："请问要什么面包？" → "全麦的"
         店员："肉饼几层？" → "双层"
         店员："加芝士吗？" → "加"
         店员："要洋葱吗？" → "不要"
         店员："酱料呢？" → "多加番茄酱"
         → 一步步确认，不容易出错
```

**生成器模式的三个角色：**

```
┌─────────────────┐      ┌─────────────────┐      ┌─────────────────┐
│     Builder     │      │    Director     │      │     Product     │
│   (建造者)       │      │   (指挥者)       │      │    (产品)       │
├─────────────────┤      ├─────────────────┤      ├─────────────────┤
│ .setTitle()     │ ←──  │ 知道构建步骤     │  ──→ │ 最终生成的      │
│ .setContent()   │      │ 按顺序调用      │      │ 复杂对象        │
│ .setWidth()     │      │ Builder 的方法   │      │                 │
│ .build()        │      │                 │      │                 │
└─────────────────┘      └─────────────────┘      └─────────────────┘
```

> **小贴士**：在前端实践中，我们通常省略 Director，直接让使用者调用 Builder 的方法。

---

### 🔧 前端实战案例一：Modal 弹窗配置生成器

#### 场景描述

封装 Ant Design 的 Modal，提供一个链式调用的 API，让创建复杂弹窗变得简单直观。

#### Step 1: 定义 Modal 配置类型

```tsx
// types/modalBuilder.ts

import { ReactNode } from 'react';

export interface ModalConfig {
  title?: ReactNode;
  content?: ReactNode;
  width?: number;
  centered?: boolean;
  closable?: boolean;
  maskClosable?: boolean;
  keyboard?: boolean;
  okText?: string;
  cancelText?: string;
  okType?: 'primary' | 'default' | 'dashed' | 'link' | 'text' | 'danger';
  okButtonProps?: Record<string, any>;
  cancelButtonProps?: Record<string, any>;
  icon?: ReactNode;
  footer?: ReactNode | null;
  onOk?: () => void | Promise<void>;
  onCancel?: () => void;
  afterClose?: () => void;
  destroyOnClose?: boolean;
  zIndex?: number;
  className?: string;
}
```

#### Step 2: 实现 Modal Builder

```tsx
// builders/ModalBuilder.tsx

import React from 'react';
import { Modal } from 'antd';
import { ExclamationCircleOutlined, CheckCircleOutlined, CloseCircleOutlined, InfoCircleOutlined } from '@ant-design/icons';
import { ModalConfig } from '../types/modalBuilder';

/**
 * Modal 弹窗配置生成器
 * 
 * 使用链式调用，一步步构建复杂的弹窗配置
 * 
 * @example
 * new ModalBuilder()
 *   .setTitle('确认删除')
 *   .setContent('确定要删除这条记录吗？')
 *   .danger()
 *   .onConfirm(handleDelete)
 *   .show();
 */
export class ModalBuilder {
  private config: ModalConfig = {};

  // ==================== 基础配置 ====================

  /**
   * 设置弹窗标题
   */
  setTitle(title: ReactNode): this {
    this.config.title = title;
    return this;
  }

  /**
   * 设置弹窗内容
   */
  setContent(content: ReactNode): this {
    this.config.content = content;
    return this;
  }

  /**
   * 设置弹窗宽度
   */
  setWidth(width: number): this {
    this.config.width = width;
    return this;
  }

  // ==================== 样式配置 ====================

  /**
   * 设置为居中显示
   */
  centered(): this {
    this.config.centered = true;
    return this;
  }

  /**
   * 设置自定义类名
   */
  setClassName(className: string): this {
    this.config.className = className;
    return this;
  }

  /**
   * 设置 z-index
   */
  setZIndex(zIndex: number): this {
    this.config.zIndex = zIndex;
    return this;
  }

  // ==================== 图标快捷方法 ====================

  /**
   * 设置为警告图标（黄色感叹号）
   */
  warning(): this {
    this.config.icon = <ExclamationCircleOutlined style={{ color: '#faad14' }} />;
    return this;
  }

  /**
   * 设置为成功图标（绿色勾）
   */
  success(): this {
    this.config.icon = <CheckCircleOutlined style={{ color: '#52c41a' }} />;
    return this;
  }

  /**
   * 设置为错误图标（红色叉）
   */
  error(): this {
    this.config.icon = <CloseCircleOutlined style={{ color: '#ff4d4f' }} />;
    return this;
  }

  /**
   * 设置为信息图标（蓝色 i）
   */
  info(): this {
    this.config.icon = <InfoCircleOutlined style={{ color: '#1890ff' }} />;
    return this;
  }

  /**
   * 设置自定义图标
   */
  setIcon(icon: ReactNode): this {
    this.config.icon = icon;
    return this;
  }

  // ==================== 按钮配置 ====================

  /**
   * 设置确认按钮文字
   */
  setOkText(text: string): this {
    this.config.okText = text;
    return this;
  }

  /**
   * 设置取消按钮文字
   */
  setCancelText(text: string): this {
    this.config.cancelText = text;
    return this;
  }

  /**
   * 设置为危险操作样式（红色确认按钮）
   */
  danger(): this {
    this.config.okType = 'danger';
    this.config.okButtonProps = { danger: true };
    return this;
  }

  /**
   * 隐藏取消按钮（只显示确认按钮）
   */
  hideCancel(): this {
    this.config.cancelButtonProps = { style: { display: 'none' } };
    return this;
  }

  /**
   * 完全隐藏底部按钮区域
   */
  hideFooter(): this {
    this.config.footer = null;
    return this;
  }

  /**
   * 设置自定义 footer
   */
  setFooter(footer: ReactNode): this {
    this.config.footer = footer;
    return this;
  }

  // ==================== 交互配置 ====================

  /**
   * 允许点击遮罩关闭
   */
  maskClosable(): this {
    this.config.maskClosable = true;
    return this;
  }

  /**
   * 禁止点击遮罩关闭
   */
  maskNotClosable(): this {
    this.config.maskClosable = false;
    return this;
  }

  /**
   * 禁用 ESC 键关闭
   */
  disableKeyboard(): this {
    this.config.keyboard = false;
    return this;
  }

  /**
   * 隐藏关闭按钮
   */
  hideCloseButton(): this {
    this.config.closable = false;
    return this;
  }

  /**
   * 关闭时销毁子组件
   */
  destroyOnClose(): this {
    this.config.destroyOnClose = true;
    return this;
  }

  // ==================== 回调配置 ====================

  /**
   * 设置确认回调
   */
  onConfirm(callback: () => void | Promise<void>): this {
    this.config.onOk = callback;
    return this;
  }

  /**
   * 设置取消回调
   */
  onCancel(callback: () => void): this {
    this.config.onCancel = callback;
    return this;
  }

  /**
   * 设置关闭后回调
   */
  onAfterClose(callback: () => void): this {
    this.config.afterClose = callback;
    return this;
  }

  // ==================== 预设模板 ====================

  /**
   * 删除确认弹窗模板
   */
  deleteConfirm(itemName?: string): this {
    this.setTitle('确认删除');
    this.setContent(itemName ? `确定要删除「${itemName}」吗？此操作不可恢复。` : '确定要删除吗？此操作不可恢复。');
    this.warning();
    this.danger();
    this.setOkText('删除');
    this.setCancelText('取消');
    this.centered();
    return this;
  }

  /**
   * 提交确认弹窗模板
   */
  submitConfirm(): this {
    this.setTitle('确认提交');
    this.setContent('确定要提交吗？提交后将进入审批流程。');
    this.info();
    this.setOkText('提交');
    this.setCancelText('取消');
    this.centered();
    return this;
  }

  /**
   * 成功提示弹窗模板
   */
  successAlert(message: string): this {
    this.setTitle('操作成功');
    this.setContent(message);
    this.success();
    this.hideCancel();
    this.setOkText('知道了');
    this.centered();
    return this;
  }

  // ==================== 构建与显示 ====================

  /**
   * 获取配置对象（不显示弹窗）
   */
  build(): ModalConfig {
    return { ...this.config };
  }

  /**
   * 显示确认弹窗
   */
  show(): ReturnType<typeof Modal.confirm> {
    return Modal.confirm(this.config);
  }

  /**
   * 显示信息弹窗
   */
  showInfo(): ReturnType<typeof Modal.info> {
    return Modal.info(this.config);
  }

  /**
   * 显示成功弹窗
   */
  showSuccess(): ReturnType<typeof Modal.success> {
    return Modal.success(this.config);
  }

  /**
   * 显示错误弹窗
   */
  showError(): ReturnType<typeof Modal.error> {
    return Modal.error(this.config);
  }

  /**
   * 显示警告弹窗
   */
  showWarning(): ReturnType<typeof Modal.warning> {
    return Modal.warning(this.config);
  }

  /**
   * 重置配置
   */
  reset(): this {
    this.config = {};
    return this;
  }
}

// 导出便捷函数
export const modalBuilder = () => new ModalBuilder();
```

#### Step 3: 业务中使用

```tsx
// pages/UserList.tsx

import React from 'react';
import { Button, Table, message } from 'antd';
import { modalBuilder } from '../builders/ModalBuilder';

export const UserList: React.FC = () => {
  
  // 场景一：简单的删除确认
  const handleDelete = (record: any) => {
    modalBuilder()
      .deleteConfirm(record.name)  // 使用预设模板
      .onConfirm(async () => {
        await deleteUser(record.id);
        message.success('删除成功');
      })
      .show();
  };

  // 场景二：自定义复杂弹窗
  const handleBatchDelete = (selectedIds: string[]) => {
    modalBuilder()
      .setTitle('批量删除确认')
      .setContent(
        <div>
          <p>您选择了 <strong>{selectedIds.length}</strong> 条记录</p>
          <p style={{ color: '#ff4d4f' }}>删除后将无法恢复，是否继续？</p>
        </div>
      )
      .warning()
      .danger()
      .setWidth(480)
      .centered()
      .maskNotClosable()      // 防止误点遮罩关闭
      .disableKeyboard()      // 防止误按 ESC 关闭
      .setOkText(`删除 ${selectedIds.length} 条`)
      .setCancelText('我再想想')
      .onConfirm(async () => {
        await batchDeleteUsers(selectedIds);
        message.success(`成功删除 ${selectedIds.length} 条记录`);
      })
      .show();
  };

  // 场景三：提交确认
  const handleSubmit = () => {
    modalBuilder()
      .submitConfirm()  // 使用预设模板
      .onConfirm(async () => {
        await submitForm();
        // 提交成功后显示成功提示
        modalBuilder()
          .successAlert('您的申请已提交，请等待审批结果。')
          .onConfirm(() => {
            // 跳转到列表页
            history.push('/list');
          })
          .show();
      })
      .show();
  };

  // 场景四：纯信息展示（无取消按钮）
  const showHelp = () => {
    modalBuilder()
      .setTitle('使用帮助')
      .setContent(
        <div>
          <h4>如何批量导入？</h4>
          <ol>
            <li>下载模板文件</li>
            <li>按模板格式填写数据</li>
            <li>点击"导入"按钮上传</li>
          </ol>
        </div>
      )
      .info()
      .hideCancel()
      .setOkText('我知道了')
      .setWidth(600)
      .show();
  };

  return (
    <div>
      <Button onClick={() => handleDelete({ id: '1', name: '张三' })}>
        删除用户
      </Button>
      <Button onClick={() => handleBatchDelete(['1', '2', '3'])}>
        批量删除
      </Button>
      <Button onClick={handleSubmit}>
        提交
      </Button>
      <Button onClick={showHelp}>
        帮助
      </Button>
    </div>
  );
};
```

---

### 🔧 前端实战案例二：HTTP 请求配置生成器

#### 场景描述

封装 axios，提供链式调用的 API，减少重复代码，统一请求配置。

#### 实现 Request Builder

```tsx
// builders/RequestBuilder.ts

import axios, { AxiosRequestConfig, AxiosResponse } from 'axios';

/**
 * HTTP 请求配置生成器
 * 
 * @example
 * const data = await new RequestBuilder()
 *   .get('/api/users')
 *   .withAuth()
 *   .withParams({ page: 1, size: 10 })
 *   .timeout(5000)
 *   .send();
 */
export class RequestBuilder<T = any> {
  private config: AxiosRequestConfig = {
    headers: {},
  };

  // ==================== HTTP 方法 ====================

  get(url: string): this {
    this.config.method = 'GET';
    this.config.url = url;
    return this;
  }

  post(url: string): this {
    this.config.method = 'POST';
    this.config.url = url;
    return this;
  }

  put(url: string): this {
    this.config.method = 'PUT';
    this.config.url = url;
    return this;
  }

  patch(url: string): this {
    this.config.method = 'PATCH';
    this.config.url = url;
    return this;
  }

  delete(url: string): this {
    this.config.method = 'DELETE';
    this.config.url = url;
    return this;
  }

  // ==================== 请求参数 ====================

  /**
   * 设置 URL 查询参数（GET 请求）
   */
  withParams(params: Record<string, any>): this {
    this.config.params = { ...this.config.params, ...params };
    return this;
  }

  /**
   * 设置请求体数据（POST/PUT/PATCH 请求）
   */
  withData(data: any): this {
    this.config.data = data;
    return this;
  }

  /**
   * 设置 JSON 数据（自动设置 Content-Type）
   */
  withJson(data: any): this {
    this.config.data = data;
    this.config.headers!['Content-Type'] = 'application/json';
    return this;
  }

  /**
   * 设置表单数据
   */
  withFormData(data: Record<string, any>): this {
    const formData = new FormData();
    Object.entries(data).forEach(([key, value]) => {
      if (value !== undefined && value !== null) {
        formData.append(key, value);
      }
    });
    this.config.data = formData;
    this.config.headers!['Content-Type'] = 'multipart/form-data';
    return this;
  }

  // ==================== 请求头配置 ====================

  /**
   * 设置自定义请求头
   */
  withHeader(key: string, value: string): this {
    this.config.headers![key] = value;
    return this;
  }

  /**
   * 设置多个请求头
   */
  withHeaders(headers: Record<string, string>): this {
    this.config.headers = { ...this.config.headers, ...headers };
    return this;
  }

  /**
   * 添加 Authorization 头（Bearer Token）
   */
  withAuth(token?: string): this {
    const authToken = token || localStorage.getItem('token') || '';
    this.config.headers!['Authorization'] = `Bearer ${authToken}`;
    return this;
  }

  /**
   * 添加请求追踪 ID
   */
  withRequestId(): this {
    this.config.headers!['X-Request-Id'] = this.generateRequestId();
    return this;
  }

  /**
   * 添加时间戳
   */
  withTimestamp(): this {
    this.config.headers!['X-Timestamp'] = String(Date.now());
    return this;
  }

  // ==================== 请求配置 ====================

  /**
   * 设置超时时间（毫秒）
   */
  timeout(ms: number): this {
    this.config.timeout = ms;
    return this;
  }

  /**
   * 携带 Cookie
   */
  withCredentials(): this {
    this.config.withCredentials = true;
    return this;
  }

  /**
   * 设置响应类型
   */
  responseType(type: 'json' | 'blob' | 'text' | 'arraybuffer'): this {
    this.config.responseType = type;
    return this;
  }

  /**
   * 设置基础 URL
   */
  baseURL(url: string): this {
    this.config.baseURL = url;
    return this;
  }

  // ==================== 快捷预设 ====================

  /**
   * 标准 API 请求预设
   * 包含：认证、请求ID、时间戳、30秒超时
   */
  standard(): this {
    return this
      .withAuth()
      .withRequestId()
      .withTimestamp()
      .timeout(30000)
      .withCredentials();
  }

  /**
   * 文件上传预设
   */
  forUpload(): this {
    return this
      .withAuth()
      .timeout(60000)  // 上传需要更长超时
      .withCredentials();
  }

  /**
   * 文件下载预设
   */
  forDownload(): this {
    return this
      .withAuth()
      .responseType('blob')
      .timeout(60000)
      .withCredentials();
  }

  // ==================== 构建与发送 ====================

  /**
   * 获取配置对象（不发送请求）
   */
  build(): AxiosRequestConfig {
    return { ...this.config };
  }

  /**
   * 发送请求并返回完整响应
   */
  async sendRaw(): Promise<AxiosResponse<T>> {
    return axios(this.config);
  }

  /**
   * 发送请求并返回数据部分
   */
  async send(): Promise<T> {
    const response = await axios(this.config);
    return response.data;
  }

  /**
   * 发送请求并处理错误
   */
  async sendSafe(): Promise<{ data: T | null; error: Error | null }> {
    try {
      const data = await this.send();
      return { data, error: null };
    } catch (error) {
      return { data: null, error: error as Error };
    }
  }

  // ==================== 私有方法 ====================

  private generateRequestId(): string {
    return `${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
  }
}

// 导出便捷函数
export const request = () => new RequestBuilder();
```

#### 业务中使用

```tsx
// services/userService.ts

import { request } from '../builders/RequestBuilder';

interface User {
  id: string;
  name: string;
  email: string;
}

interface PageResult<T> {
  list: T[];
  total: number;
}

/**
 * 获取用户列表
 */
export const getUserList = async (params: { page: number; size: number }) => {
  return request()
    .get('/api/users')
    .standard()                    // 使用标准预设
    .withParams(params)
    .send<PageResult<User>>();
};

/**
 * 获取用户详情
 */
export const getUserById = async (id: string) => {
  return request()
    .get(`/api/users/${id}`)
    .standard()
    .send<User>();
};

/**
 * 创建用户
 */
export const createUser = async (data: Omit<User, 'id'>) => {
  return request()
    .post('/api/users')
    .standard()
    .withJson(data)
    .send<User>();
};

/**
 * 更新用户
 */
export const updateUser = async (id: string, data: Partial<User>) => {
  return request()
    .put(`/api/users/${id}`)
    .standard()
    .withJson(data)
    .send<User>();
};

/**
 * 删除用户
 */
export const deleteUser = async (id: string) => {
  return request()
    .delete(`/api/users/${id}`)
    .standard()
    .send<void>();
};

/**
 * 上传头像
 */
export const uploadAvatar = async (file: File) => {
  return request()
    .post('/api/users/avatar')
    .forUpload()                   // 使用上传预设
    .withFormData({ file })
    .send<{ url: string }>();
};

/**
 * 导出用户列表
 */
export const exportUsers = async (params: { ids?: string[] }) => {
  return request()
    .post('/api/users/export')
    .forDownload()                 // 使用下载预设
    .withJson(params)
    .sendRaw();                    // 返回完整响应以获取 headers
};
```

---

### 🔧 前端实战案例三：表单校验规则生成器

#### 场景描述

Ant Design Form 的 rules 配置经常很长，用 Builder 模式可以让校验规则的配置更加优雅。

```tsx
// builders/RuleBuilder.ts

import { Rule } from 'antd/es/form';

/**
 * 表单校验规则生成器
 * 
 * @example
 * const rules = new RuleBuilder()
 *   .required('请输入用户名')
 *   .minLength(2, '用户名至少2个字符')
 *   .maxLength(20, '用户名最多20个字符')
 *   .pattern(/^[a-zA-Z0-9_]+$/, '只能包含字母、数字和下划线')
 *   .build();
 */
export class RuleBuilder {
  private rules: Rule[] = [];

  /**
   * 必填校验
   */
  required(message = '此项为必填项'): this {
    this.rules.push({ required: true, message });
    return this;
  }

  /**
   * 最小长度
   */
  minLength(min: number, message?: string): this {
    this.rules.push({ 
      min, 
      message: message || `最少输入 ${min} 个字符` 
    });
    return this;
  }

  /**
   * 最大长度
   */
  maxLength(max: number, message?: string): this {
    this.rules.push({ 
      max, 
      message: message || `最多输入 ${max} 个字符` 
    });
    return this;
  }

  /**
   * 长度范围
   */
  lengthRange(min: number, max: number, message?: string): this {
    this.rules.push({ 
      min, 
      max, 
      message: message || `请输入 ${min}-${max} 个字符` 
    });
    return this;
  }

  /**
   * 正则校验
   */
  pattern(regex: RegExp, message: string): this {
    this.rules.push({ pattern: regex, message });
    return this;
  }

  /**
   * 邮箱格式
   */
  email(message = '请输入有效的邮箱地址'): this {
    this.rules.push({ type: 'email', message });
    return this;
  }

  /**
   * URL 格式
   */
  url(message = '请输入有效的 URL'): this {
    this.rules.push({ type: 'url', message });
    return this;
  }

  /**
   * 数字类型
   */
  number(message = '请输入数字'): this {
    this.rules.push({ type: 'number', message });
    return this;
  }

  /**
   * 整数
   */
  integer(message = '请输入整数'): this {
    this.rules.push({ type: 'integer', message });
    return this;
  }

  /**
   * 数值范围
   */
  range(min: number, max: number, message?: string): this {
    this.rules.push({
      type: 'number',
      min,
      max,
      message: message || `请输入 ${min}-${max} 之间的数字`,
    });
    return this;
  }

  /**
   * 手机号（中国大陆）
   */
  mobile(message = '请输入有效的手机号'): this {
    this.rules.push({
      pattern: /^1[3-9]\d{9}$/,
      message,
    });
    return this;
  }

  /**
   * 身份证号
   */
  idCard(message = '请输入有效的身份证号'): this {
    this.rules.push({
      pattern: /^[1-9]\d{5}(18|19|20)\d{2}(0[1-9]|1[0-2])(0[1-9]|[12]\d|3[01])\d{3}[\dXx]$/,
      message,
    });
    return this;
  }

  /**
   * 只允许字母
   */
  onlyLetters(message = '只能输入字母'): this {
    this.rules.push({
      pattern: /^[a-zA-Z]+$/,
      message,
    });
    return this;
  }

  /**
   * 只允许字母和数字
   */
  alphanumeric(message = '只能输入字母和数字'): this {
    this.rules.push({
      pattern: /^[a-zA-Z0-9]+$/,
      message,
    });
    return this;
  }

  /**
   * 不允许空格
   */
  noSpaces(message = '不能包含空格'): this {
    this.rules.push({
      pattern: /^\S*$/,
      message,
    });
    return this;
  }

  /**
   * 自定义校验函数
   */
  custom(validator: (value: any) => Promise<void> | void, message?: string): this {
    this.rules.push({
      validator: async (_, value) => {
        try {
          await validator(value);
        } catch (error) {
          throw new Error(message || (error as Error).message);
        }
      },
    });
    return this;
  }

  /**
   * 与另一个字段相同（如确认密码）
   */
  sameAs(fieldName: string, message?: string): this {
    this.rules.push(({ getFieldValue }) => ({
      validator(_, value) {
        if (!value || getFieldValue(fieldName) === value) {
          return Promise.resolve();
        }
        return Promise.reject(new Error(message || '两次输入不一致'));
      },
    }));
    return this;
  }

  /**
   * 构建规则数组
   */
  build(): Rule[] {
    return [...this.rules];
  }
}

// 导出便捷函数
export const rules = () => new RuleBuilder();
```

#### 业务中使用

```tsx
// pages/RegisterForm.tsx

import React from 'react';
import { Form, Input, Button } from 'antd';
import { rules } from '../builders/RuleBuilder';

export const RegisterForm: React.FC = () => {
  const [form] = Form.useForm();

  return (
    <Form form={form} layout="vertical">
      {/* 用户名：必填 + 长度限制 + 格式要求 */}
      <Form.Item
        name="username"
        label="用户名"
        rules={rules()
          .required('请输入用户名')
          .lengthRange(4, 20, '用户名需要 4-20 个字符')
          .alphanumeric('用户名只能包含字母和数字')
          .build()}
      >
        <Input placeholder="请输入用户名" />
      </Form.Item>

      {/* 邮箱：必填 + 邮箱格式 */}
      <Form.Item
        name="email"
        label="邮箱"
        rules={rules()
          .required('请输入邮箱')
          .email('请输入有效的邮箱地址')
          .build()}
      >
        <Input placeholder="请输入邮箱" />
      </Form.Item>

      {/* 手机号：必填 + 手机格式 */}
      <Form.Item
        name="mobile"
        label="手机号"
        rules={rules()
          .required('请输入手机号')
          .mobile('请输入有效的手机号')
          .build()}
      >
        <Input placeholder="请输入手机号" />
      </Form.Item>

      {/* 密码：必填 + 长度 + 复杂度 */}
      <Form.Item
        name="password"
        label="密码"
        rules={rules()
          .required('请输入密码')
          .minLength(8, '密码至少 8 个字符')
          .pattern(
            /^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)/,
            '密码需要包含大小写字母和数字'
          )
          .noSpaces('密码不能包含空格')
          .build()}
      >
        <Input.Password placeholder="请输入密码" />
      </Form.Item>

      {/* 确认密码：必填 + 与密码相同 */}
      <Form.Item
        name="confirmPassword"
        label="确认密码"
        rules={rules()
          .required('请确认密码')
          .sameAs('password', '两次密码输入不一致')
          .build()}
      >
        <Input.Password placeholder="请再次输入密码" />
      </Form.Item>

      {/* 年龄：数字范围 */}
      <Form.Item
        name="age"
        label="年龄"
        rules={rules()
          .required('请输入年龄')
          .integer('年龄必须是整数')
          .range(18, 120, '年龄需要在 18-120 之间')
          .build()}
      >
        <Input placeholder="请输入年龄" type="number" />
      </Form.Item>

      <Form.Item>
        <Button type="primary" htmlType="submit">
          注册
        </Button>
      </Form.Item>
    </Form>
  );
};
```

---

### 📊 模式总结

#### ✅ 优点

| 优点 | 说明 |
|------|------|
| **链式调用** | 代码可读性强，像"说话"一样自然 |
| **渐进式构建** | 可以分步骤设置，不用一次性传所有参数 |
| **减少参数错误** | 方法名即含义，不会传错位置 |
| **支持预设模板** | 常用配置可以封装成预设方法 |
| **灵活组合** | 可以根据需要选择性设置属性 |
| **易于扩展** | 新增配置项只需添加新方法 |

#### ❌ 缺点

| 缺点 | 说明 |
|------|------|
| **代码量增加** | 需要为每个属性编写设置方法 |
| **必须调用 build** | 容易忘记最后调用 build() 或 send() |
| **调试稍复杂** | 链式调用时单步调试不太方便 |
| **简单场景过度** | 如果配置项很少，用 Builder 显得繁琐 |

#### 🎯 适用场景

| 场景 | 示例 |
|------|------|
| **复杂配置对象** | Modal 配置、Drawer 配置、Notification 配置 |
| **HTTP 请求构建** | 请求头、参数、超时等配置组合 |
| **表单校验规则** | 多条校验规则的组合 |
| **查询条件构建** | SQL/API 查询条件的拼装 |
| **组件属性构建** | 复杂组件的 props 配置 |

#### 🚫 不适用场景

- 配置项很少（< 5 个），直接传对象更简单
- 配置项之间有严格的依赖顺序
- 团队不熟悉链式调用风格

---

### 🤔 选择建议：什么时候用生成器模式？

问自己这几个问题：

```
1. 你的配置对象是否有超过 5 个可选属性？
   → 是的话，考虑用生成器模式

2. 你是否经常只需要设置部分属性，其他用默认值？
   → 是的话，考虑用生成器模式

3. 你是否有一些"常用配置组合"想要复用？
   → 是的话，考虑用生成器模式（可以封装成预设方法）

4. 你的参数列表是否经常让人困惑"这个 true 是什么意思"？
   → 是的话，考虑用生成器模式（方法名自带语义）
```

---

### 📁 文件结构参考

```
src/
├── types/
│   └── modalBuilder.ts           # 类型定义
├── builders/
│   ├── ModalBuilder.tsx          # Modal 配置生成器
│   ├── RequestBuilder.ts         # HTTP 请求生成器
│   └── RuleBuilder.ts            # 表单校验规则生成器
├── services/
│   └── userService.ts            # 使用 RequestBuilder 的服务
└── pages/
    ├── UserList.tsx              # 使用 ModalBuilder 的页面
    └── RegisterForm.tsx          # 使用 RuleBuilder 的表单
```

---

### 🔄 与其他创建型模式的选择

| 场景 | 推荐模式 |
|------|---------|
| 根据类型创建不同组件 | 工厂方法模式 |
| 创建一整套相关组件 | 抽象工厂模式 |
| 分步骤构建复杂配置 | 生成器模式 |
| 全局唯一实例 | 单例模式 |
| 基于现有对象创建副本 | 原型模式 |

---

> **下一章预告：《单例模式》—— 全局只有一个"老婆"的正确打开方式**
> 
> 我们将讲解如何优雅地管理全局状态、弹窗管理器、API 客户端等"全局唯一"的场景。

---

*如有疑问或建议，欢迎交流！*

