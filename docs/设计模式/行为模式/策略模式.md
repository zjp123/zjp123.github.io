# 前端设计模式实战手册

> 通过"深入浅出"的方式，讲解设计模式在前端（React + Ant Design）中的实际应用

---

## 第一章：策略模式（Strategy Pattern）

### 一句话大白话

**就像点外卖选择配送方式**：你可以选骑手送、无人机送、或者自取，不管选哪种，你都能拿到外卖。外卖平台不需要关心具体怎么送，它只需要调用"配送"这个动作就行了。

### 前端痛点

想象一下，你在做一个电商后台的优惠券系统，需要根据不同类型的优惠券计算折扣：

```typescript
// ❌ 没有使用策略模式的代码：一堆 if-else 的灾难
function calculateDiscount(type, price) {
  if (type === 'fullReduction') {
    // 满减：满100减20
    if (price >= 100) return 20;
    return 0;
  } else if (type === 'discount') {
    // 折扣：9折
    return price * 0.1;
  } else if (type === 'voucher') {
    // 代金券：直减10元
    return 10;
  } else if (type === 'firstOrder') {
    // 新人专享：5折
    return price * 0.5;
  } else if (type === 'vip') {
    // VIP会员：8折
    return price * 0.2;
  }
  // ... 新增优惠类型时，继续加 else if
  return 0;
}
```

**这段代码的问题：**
1. 💣 **违反开闭原则**：每次新增优惠类型，都要改这个函数，容易引入 bug
2. 🔗 **耦合严重**：所有计算逻辑都耦合在一起，牵一发动全身
3. 📖 **难以测试**：要测一个优惠类型，必须把整个函数都跑一遍
4. 🤯 **可读性差**：多层 if-else 嵌套，维护时让人头大

### 模式核心概念

**策略模式的本质**：把"做什么"和"怎么做"分开。

- **策略（Strategy）**：定义一系列算法/规则，把它们封装起来，让它们可以互相替换
- **上下文（Context）**：不关心具体用哪个策略，只负责调用

**类比**：你是公司老板（Context），需要完成一个项目。你不需要自己写代码，只需要根据项目需求，选择合适的外包团队（Strategy）去完成就行。

### 前端实战案例

#### 场景：Ant Design 表单的多种校验策略

我们要实现一个注册表单，不同字段有不同的校验规则：

```typescript
// ✅ 使用策略模式重构

import React from 'react';
import { Form, Input, Button, message } from 'antd';

// 1️⃣ 定义策略接口（TypeScript）
interface ValidationStrategy {
  validate: (value: string) => { valid: boolean; message?: string };
}

// 2️⃣ 实现各种具体策略
class PhoneStrategy implements ValidationStrategy {
  validate(value: string) {
    const phoneReg = /^1[3-9]\d{9}$/;
    return {
      valid: phoneReg.test(value),
      message: phoneReg.test(value) ? undefined : '请输入正确的手机号格式'
    };
  }
}

class EmailStrategy implements ValidationStrategy {
  validate(value: string) {
    const emailReg = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
    return {
      valid: emailReg.test(value),
      message: emailReg.test(value) ? undefined : '请输入正确的邮箱格式'
    };
  }
}

class PasswordStrategy implements ValidationStrategy {
  validate(value: string) {
    // 密码要求：8-16位，包含数字和字母
    const hasNumber = /\d/.test(value);
    const hasLetter = /[a-zA-Z]/.test(value);
    const validLength = value.length >= 8 && value.length <= 16;
    
    const valid = hasNumber && hasLetter && validLength;
    return {
      valid,
      message: valid ? undefined : '密码需8-16位，包含数字和字母'
    };
  }
}

class IdCardStrategy implements ValidationStrategy {
  validate(value: string) {
    const idCardReg = /(^\d{15}$)|(^\d{18}$)|(^\d{17}(\d|X|x)$)/;
    return {
      valid: idCardReg.test(value),
      message: idCardReg.test(value) ? undefined : '请输入正确的身份证号'
    };
  }
}

// 3️⃣ 策略管理器（Context）：统一调度不同的策略
class Validator {
  private strategy: ValidationStrategy;

  constructor(strategy: ValidationStrategy) {
    this.strategy = strategy;
  }

  // 允许动态切换策略
  setStrategy(strategy: ValidationStrategy) {
    this.strategy = strategy;
  }

  // 执行校验
  validate(value: string) {
    return this.strategy.validate(value);
  }
}

// 4️⃣ 在 React 组件中使用
const RegisterForm: React.FC = () => {
  const [form] = Form.useForm();

  // 创建策略映射表（方便扩展）
  const strategies = {
    phone: new PhoneStrategy(),
    email: new EmailStrategy(),
    password: new PasswordStrategy(),
    idCard: new IdCardStrategy(),
  };

  // 自定义校验规则生成器
  const createValidator = (strategyKey: keyof typeof strategies) => {
    return (_: any, value: string) => {
      if (!value) {
        return Promise.reject('该字段不能为空');
      }
      
      const validator = new Validator(strategies[strategyKey]);
      const result = validator.validate(value);
      
      if (result.valid) {
        return Promise.resolve();
      }
      return Promise.reject(result.message);
    };
  };

  const onFinish = (values: any) => {
    message.success('注册成功！');
    console.log('表单数据:', values);
  };

  return (
    <Form
      form={form}
      layout="vertical"
      onFinish={onFinish}
      style={{ maxWidth: 400, margin: '50px auto' }}
    >
      <Form.Item
        label="手机号"
        name="phone"
        rules={[{ validator: createValidator('phone') }]}
      >
        <Input placeholder="请输入手机号" />
      </Form.Item>

      <Form.Item
        label="邮箱"
        name="email"
        rules={[{ validator: createValidator('email') }]}
      >
        <Input placeholder="请输入邮箱" />
      </Form.Item>

      <Form.Item
        label="密码"
        name="password"
        rules={[{ validator: createValidator('password') }]}
      >
        <Input.Password placeholder="请输入密码" />
      </Form.Item>

      <Form.Item
        label="身份证号"
        name="idCard"
        rules={[{ validator: createValidator('idCard') }]}
      >
        <Input placeholder="请输入身份证号" />
      </Form.Item>

      <Form.Item>
        <Button type="primary" htmlType="submit" block>
          注册
        </Button>
      </Form.Item>
    </Form>
  );
};

export default RegisterForm;
```

#### 🎯 使用策略模式后的好处

1. **新增校验规则超简单**：只需新建一个策略类，无需修改原有代码
   ```typescript
   // 新增银行卡校验，原代码零改动
   class BankCardStrategy implements ValidationStrategy {
     validate(value: string) {
       const bankCardReg = /^\d{16,19}$/;
       return {
         valid: bankCardReg.test(value),
         message: bankCardReg.test(value) ? undefined : '请输入16-19位银行卡号'
       };
     }
   }
   
   // 在策略映射表中添加即可
   const strategies = {
     // ...原有策略
     bankCard: new BankCardStrategy(), // 一行搞定
   };
   ```

2. **测试变得轻松**：每个策略独立测试，互不干扰
   ```typescript
   // 单元测试示例
   test('PhoneStrategy 应该正确校验手机号', () => {
     const strategy = new PhoneStrategy();
     expect(strategy.validate('13812345678').valid).toBe(true);
     expect(strategy.validate('12345').valid).toBe(false);
   });
   ```

3. **业务逻辑清晰**：一眼就能看懂每个策略做什么

---

#### 💡 再举一个场景：优惠券计算（开头的痛点重构版）

```typescript
import React, { useState } from 'react';
import { Card, Radio, InputNumber, Button, Statistic } from 'antd';

// 策略接口
interface DiscountStrategy {
  calculate: (originalPrice: number) => number;
  description: string;
}

// 满减策略
class FullReductionStrategy implements DiscountStrategy {
  description = '满100减20';
  
  calculate(originalPrice: number) {
    return originalPrice >= 100 ? 20 : 0;
  }
}

// 折扣策略
class PercentDiscountStrategy implements DiscountStrategy {
  private percent: number;
  description: string;

  constructor(percent: number) {
    this.percent = percent;
    this.description = `${percent * 10}折优惠`;
  }

  calculate(originalPrice: number) {
    return originalPrice * (1 - this.percent);
  }
}

// 代金券策略
class VoucherStrategy implements DiscountStrategy {
  private amount: number;
  description: string;

  constructor(amount: number) {
    this.amount = amount;
    this.description = `直减${amount}元`;
  }

  calculate(originalPrice: number) {
    return Math.min(this.amount, originalPrice);
  }
}

// 新人专享策略
class FirstOrderStrategy implements DiscountStrategy {
  description = '新人5折';

  calculate(originalPrice: number) {
    return originalPrice * 0.5;
  }
}

// 优惠券计算器组件
const CouponCalculator: React.FC = () => {
  const [originalPrice, setOriginalPrice] = useState<number>(150);
  const [selectedCoupon, setSelectedCoupon] = useState<string>('fullReduction');

  // 策略映射
  const strategies: Record<string, DiscountStrategy> = {
    fullReduction: new FullReductionStrategy(),
    discount9: new PercentDiscountStrategy(0.1),
    discount8: new PercentDiscountStrategy(0.2),
    voucher10: new VoucherStrategy(10),
    voucher30: new VoucherStrategy(30),
    firstOrder: new FirstOrderStrategy(),
  };

  // 计算优惠
  const discountAmount = strategies[selectedCoupon].calculate(originalPrice);
  const finalPrice = originalPrice - discountAmount;

  return (
    <Card title="优惠券计算器" style={{ maxWidth: 500, margin: '50px auto' }}>
      <div style={{ marginBottom: 20 }}>
        <label>商品原价：</label>
        <InputNumber
          value={originalPrice}
          onChange={(val) => setOriginalPrice(val || 0)}
          min={0}
          prefix="¥"
          style={{ width: 200 }}
        />
      </div>

      <div style={{ marginBottom: 20 }}>
        <label>选择优惠券：</label>
        <Radio.Group
          value={selectedCoupon}
          onChange={(e) => setSelectedCoupon(e.target.value)}
        >
          {Object.entries(strategies).map(([key, strategy]) => (
            <Radio.Button key={key} value={key}>
              {strategy.description}
            </Radio.Button>
          ))}
        </Radio.Group>
      </div>

      <Card style={{ backgroundColor: '#f0f2f5' }}>
        <Statistic
          title="优惠金额"
          value={discountAmount}
          precision={2}
          prefix="¥"
          valueStyle={{ color: '#cf1322' }}
        />
        <Statistic
          title="实付金额"
          value={finalPrice}
          precision={2}
          prefix="¥"
          valueStyle={{ color: '#3f8600' }}
          style={{ marginTop: 16 }}
        />
      </Card>
    </Card>
  );
};

export default CouponCalculator;
```

### 模式总结

#### ✅ 优点

1. **符合开闭原则**：对扩展开放（新增策略容易），对修改关闭（原有代码不动）
2. **消除条件语句**：避免大量的 if-else 或 switch-case
3. **提高可维护性**：每个策略独立，改一个不影响其他
4. **便于单元测试**：策略之间互不依赖，测试很方便
5. **提高代码复用**：策略可以在不同场景复用

#### ❌ 缺点

1. **增加类的数量**：每个策略都是一个类，小项目可能显得"重"
2. **使用者需要了解所有策略**：需要知道有哪些策略可选

#### 🎯 适用场景

策略模式特别适合以下前端场景：

| 场景 | 说明 | 典型例子 |
|------|------|----------|
| **表单校验** | 不同字段不同校验规则 | 手机号、邮箱、身份证、密码强度 |
| **支付方式** | 多种支付渠道 | 支付宝、微信、银行卡、余额 |
| **数据处理** | 不同格式的数据导入/导出 | Excel、CSV、JSON、XML |
| **图表渲染** | 根据数据类型选择图表 | 折线图、柱状图、饼图、雷达图 |
| **权限控制** | 不同角色不同权限策略 | 管理员、普通用户、游客 |
| **动画效果** | 不同的动画曲线 | linear、ease-in、ease-out、bounce |
| **价格计算** | 多种优惠规则 | 满减、折扣、代金券、积分抵扣 |

#### 🤔 什么时候该用策略模式？

**三个判断标准：**

1. **是否有多个条件分支？** 
   - 如果你的代码有3个以上的 if-else 或 switch-case，考虑策略模式

2. **这些分支做的是"同一件事"吗？**
   - 比如都是"计算价格"、都是"校验数据"、都是"渲染图表"

3. **未来会新增更多分支吗？**
   - 如果业务可能扩展（比如新增优惠类型、新增支付方式），用策略模式

**快速决策树：**

```
你的代码有很多 if-else 吗？
    ├─ 是 → 这些分支做的是同类事情吗？
    │        ├─ 是 → 未来可能新增更多分支吗？
    │        │        ├─ 是 → ✅ 用策略模式！
    │        │        └─ 否 → 🤔 看代码量，复杂就用
    │        └─ 否 → ❌ 不适合，考虑其他模式
    └─ 否 → ❌ 暂时不需要
```

---

### 💬 小结

策略模式就像是给你的代码装上了"可更换的零件"，需要什么功能，换上对应的零件就行，而不是把所有功能都焊死在一起。

在实际项目中，策略模式能让你的代码：
- 从"一大坨 if-else"变成"一个个清晰的策略类"
- 从"改一处牵连全局"变成"新增策略零风险"
- 从"难以测试"变成"策略独立，随便测"

下一章，我们将学习**工厂模式**，看看如何优雅地创建各种复杂对象，避免 `new` 满天飞的尴尬。

---

> 💡 **思考题**：在你的项目中，有没有哪些地方用了大量的 if-else？试着用策略模式重构一下，看看代码是不是清爽多了？


